# Steel-folding test corpus — intentionally vulnerable code
tests/fold*_vulnerable_samples.py

# Doji source files contain rule descriptions that reference dangerous
# functions (e.g. "pickle.loads() can execute...") — self-triggering
dojigiri/languages.py
dojigiri/detector.py
